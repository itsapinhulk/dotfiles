version: 2.1

jobs:
  test-python:
    docker:
      - image: cimg/python:3.14
    steps:
      - checkout
      - run:
          command: python3 test/test_deploy_dotfiles.py

workflows:
  tests:
    jobs:
      - test-python
